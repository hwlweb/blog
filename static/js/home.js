"use strict";

define(function (require, exports, module) {
    //var  updateMarkdown = function(text, dom) {
    //    $(dom).html(marked(text));
    //};

    exports.init = function () {
        //$('.article-body').each(function () {
        //    var html = $(this).html();
        //    updateMarkdown(html,this);
        //});
    }
})